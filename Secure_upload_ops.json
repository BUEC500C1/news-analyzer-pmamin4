// uploading files

def upload(file):
  
  create log
  if filetype is invalid:
    return 'invalid type'
  if filetype is valid:
    log(file)
    return 'saved'

def convert_to_form(file, form):

  if form is invalid:
    return 'invalid'
  elif filetype is invalid:
    return 'invalid'
  else:
    file_in_form = newfile
    return upload(newfile)


enable_viewing(file, viewer)

enable_editing(file, editor)

enable_commenting(file, commentator)
