
def textfind(text,document):
    
    count = 0

    for all 'text' in document:
      count +=1

    return count


def create_document(doctype):

    if doctype is not valid:
        return 'invalid document type'
    text = input_text('input text here')
    if text not text type:
      return 'invalid characters'
    else:
      merge(doctype,text)



def save_document(document):

## use [] as a variable holder (this would be a way to hold/save a document
    if document is not text type:
        return 'invalid type'

    text_storage = [document]
    
    return 'document saved'



def view_document(document):

    if document is not valid:
        return 'invalid document form'
    else:
        return 'document opened'



def definition(word):

  define = 'definition of the word with a link to source' + hyperlink

  return define



def hyperlink(text,source_url):

  if source_url is not valid:
    return 'invalid url'
  url = input(source_url)
  
  text = input('text')
  createhyperlink()


  return 'source linked'


def math_operations(num1,operation,num2):

    op = ['+', '-', '/', '*']
    
    if operation not in op:
       return 'invalid operation'
    
    elif num1 or num2 are not float or int types:
       return 'invalid numbers'

    elif operation == '+'
          return num1 + num2

    elif operation == '-':
          return num1 - num2
    elif operation == '/':
          return num1 / num2
    else:
        if operation == '*'
            return num1 * num2


  

